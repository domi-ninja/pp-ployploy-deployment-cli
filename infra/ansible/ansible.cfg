[defaults]
inventory = inventory/prod.example.yml
host_key_checking = True
interpreter_python = auto_silent
roles_path = roles
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
