---
- name: restart forgejo
  ansible.builtin.command: "docker compose -f {{ forgejo_compose_dir }}/compose.yml up -d --remove-orphans"
  changed_when: true

- name: Reload caddy for Forgejo route
  listen: Reload caddy
  ansible.builtin.systemd:
    name: caddy
    state: reloaded
